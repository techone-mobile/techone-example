class FlashDealModel {
  String id, name, thumbnail;
  BigInt saleMoney, oldMoney;
  int sold, total;

  FlashDealModel(this.id, this.name, this.thumbnail, this.saleMoney, this.oldMoney, this.sold, this.total);
}
